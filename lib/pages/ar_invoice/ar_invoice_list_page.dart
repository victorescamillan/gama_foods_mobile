import 'package:flutter/material.dart';

class ARInvoiceListPage extends StatefulWidget {
  const ARInvoiceListPage({Key? key}) : super(key: key);

  @override
  _ARInvoiceListPageState createState() => _ARInvoiceListPageState();
}

class _ARInvoiceListPageState extends State<ARInvoiceListPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Container());
  }
}
